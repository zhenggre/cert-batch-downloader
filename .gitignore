# ===== 证书批量下载器 · .gitignore =====
# 本仓库只收工具源码。以下内容不纳入版本库。

# 1) 交付包：PyInstaller 打包产物（二进制，每次构建都变，约 25MB）
#    ⚠ 必须带 **/ 前缀。仓库根下并没有这一层，实际路径是
#    「下载器程序/① 双击就能用…/」。不带前缀的规则匹配不到，等于没写 ——
#    2026-09-22 复盘时实测：加了前缀才真正拦得住。
**/① 双击就能用（直接用不用装任何东西）/

# 2) 打包/运行环境：第三方 Python 库（约 40-80MB，非本项目代码）
**/runtime/
**/_runtime_dl/
**/_internal/
**/*.exe

# 3) 构建中间产物（PyInstaller 工程文件）
**/build/

# 4) Python 缓存
**/__pycache__/
**/*.pyc

# 5) 运行时状态 / 快捷方式（由程序运行时生成，非源码）
**/运行中.json
**/界面地址.txt
**/打开界面.url
**/用户设置.json

# 6) 真实私有配置与备份（请用 *.example.* 模板自行生成）
**/config.json
**/config.json.bak
*.改前备份*

# 7) 学生 / 学校数据（按作者要求不进库；含姓名、学校、比赛结果）
**/学校名映射.csv
**/证书汇总清单*.xlsx
**/下载明细*.csv
**/学校名归并报告*.txt
**/运行日志*.txt
**/比赛信息.json

# 8) 历史版本 / 发给同事的副本（含另一份打包二进制）
**/历史版本/
**/发给同事/

# 9) 测试 / 构建日志与截图
**/*.log
**/docs_test.txt
**/error_help_test.txt
**/probe_test.txt
**/*_test.txt
**/shot_*.png

# 10) 故障诊断报告（含学生姓名与学校，属于个人材料，不进库）
**/故障诊断*.txt

# 11) 演示名单（号码是构造的，但格式合法，公开出去容易被误认成真实学生数据）
**/名单示例_演示用.xlsx

# 12) 内部文档与私人便签（含与使用者的需求往来、给同事的交接说明，不进公开库）
**/使用说明.md
**/使用说明.txt
**/先看我.md
**/明天先看我.md
**/文件清单.md
**/保存位置说明.txt

# 13) 真实查询页快照（含实际赛事名称与站点标识，属抓取到的现场数据）
**/fixture_比赛查询页.html
**/fixture_金数据新版_RSC.txt

# 14) 本地 git 历史备份（重建历史时留的存档，不入库）
*.git.bak_*/
.git.bak_*/

# 15) 本地脱敏处理留下的备份文件
**/*.bak_before_sanitize

# 16) 测试运行留下的临时目录 / 中间产物（跑测试即可重新生成，不进库）
**/_tmp_*/
**/_tmp_*.json
**/诊断测试_*/
**/probe_test.txt
